export default function boxReducer(state ={boxes: []}, action) {
    // return state;
    // return action.payload; 
    // to be able to see that our action was coming in correctly and that the store could properly be updated by our reducer.
}